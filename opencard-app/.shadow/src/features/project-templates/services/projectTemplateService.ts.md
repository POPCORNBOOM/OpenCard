# 项目模板结构契约

模板内容不是项目结构的替代来源。创建项目时必须先复制模板的用户内容，再通过统一的
项目结构服务补齐并校验 `.opencard`；模板不得自行维护另一套初始化逻辑。

内置模板源码应携带五个规范项目文档，便于直接检查和打包验证；三个受管资源目录允许
在源码包中为空，因为文件系统不保存空目录，但最终项目必须由初始化服务创建它们。
构建产物中的模板目录必须只反映当前源码，不能让已删除或移动的旧根目录 `.oc*` 文件
从历史构建暂存中继续进入新项目。
