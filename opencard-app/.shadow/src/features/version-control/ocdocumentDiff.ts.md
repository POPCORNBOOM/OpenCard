# OpenCard 文档差异语义

- 比较器先经过当前 `normalizeCardDocument`，因此格式、键顺序、未知字段和可默认字段不应制造差异；这是当前模型比较，不是原始 JSON 审计。
- 区块和实例必须按持久化 `id` 对齐。数组下标只能表达同一父容器内的相对顺序，不能作为对象身份。
- 比较器是纯函数，不读取 Git、项目 Store 或资源；历史资源只服务于预览，资源失败不能改变结构化差异结果。
