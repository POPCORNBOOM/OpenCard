# 项目时间线的语义

- 时间线只读 Rust Git 服务看到的磁盘状态；不会为了读取历史保存编辑器草稿，也不会把“未保存”伪装成提交。
- 项目根没有自己的 `.git` 时显示未初始化，即使父目录是仓库也不能向上误判。
- 请求编号是必要的边界：切换项目后，旧仓库的异步响应不能覆盖当前项目的时间线。
- 时间线是提交列表，不是文件更改树；提交选择只改变选择状态，本阶段不展开差异或编辑提交。
