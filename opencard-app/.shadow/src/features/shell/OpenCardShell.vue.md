# 壳层时间线边界

- 侧栏时间线是当前项目文件 Git 提交的只读投影；它跟随活动编辑 session，但与文件树选中项和未保存草稿保持独立。
- 时间线刷新只通过 `useProjectTimeline` 进入 Git 服务；壳层不直接调用 `invoke('git_*')`，也不自行解释 Git 错误文案。
- 本阶段点击提交只改变树选择，不打开编辑器、不展开文件差异；后续交互必须先扩展时间线领域接口再接入。
