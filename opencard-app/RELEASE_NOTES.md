## 改进

- 新建项目的左侧资源目录现在只保留模板、图标包和自定义块三棵树；内置模板直接显示在根部，我的模板集中在独立分组中，图标包与自定义块则直接平铺。
- 自定义块现在可以导入到 OpenCard 资源库，后续创建项目时可从列表中加入并注册；与模板中已有相同 Key 的自定义块会被拒绝，避免字段契约冲突。
- 新建项目的模板详情和创建表单改为更紧凑的工作区排版，名称、入口、图标包和自定义块信息可以直接对照确认。
