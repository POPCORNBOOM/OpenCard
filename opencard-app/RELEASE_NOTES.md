- 项目内部资源配置现在统一使用 `.opencard` 下的 `project.json`、`fonts/fonts.json`、`icons/icons.json`、`blocks/blocks.json` 和 `locale.json`；包使用 `.ocpack`，自定义块结构使用 `.ocblock`。内置模板已移除旧格式配置文件，新建项目不再夹带这些文件。
## 新增
- 项目文件列表标题栏现在提供文件夹按钮，可直接在系统文件管理器中显示当前项目文件夹。
- 侧栏的“项目”“字典”“字体”“图标”和“包”现在会显示各自对应的项目文件路径（如 `.opencard/fonts/fonts.json`），便于直接找到这些受管理的文件。
- 包管理页面现在可以一键从 GitHub Release 获取并安装项目中缺失或版本不符的包，并显示批量安装进度和结果提示；单个包安装失败的原因会直接显示在输出面板，点击条目可复制完整详情。
- 滑块现在支持显示指定数值的刻度，并会区分当前位置之前和之后的刻度。
- 新增自动保存设置，可在文件菜单中随时开启或关闭，并配置自动保存间隔；自动保存只处理已落盘且已修改的文件，不会替草稿触发首次保存。
- 图片块现在会按项目的远程资源策略缓存允许的 HTTPS 图片；首次下载完成后自动显示本地缓存，网络暂时不可用时继续使用已有缓存，被安全策略阻止的远程图片会显示在问题列表中。
- 设置的“工作区”页面现在可以调整自定义块最大递归深度，避免嵌套结构过深时持续展开。
- CDE 切换到数据表视图时，数据表会自动定位在编辑区域右下方并占用约一半屏幕高度；返回设计视图后，卡面会自动居中并适配视口。

- 开发环境的帮助菜单新增 CDE 调试开关，可持续隐藏编辑器浮层，方便观察纯画布布局。
- 开发环境的帮助菜单新增两个 CDE 调试开关，可分别暴露上层背景，或将画布切换为不响应点击的纯展示状态。
- 包管理页面现在支持添加多个本地 `.ocpack` 或远程包声明（`github:作者/包名@标签`，可一行一个或用分号分隔），并集中查看当前项目需要的包及版本；在侧栏“包”下选中子包会打开专用信息查看器并持续高光，通过文件管理器增删包目录后列表会自动同步。
- 文件菜单现在提供“制作包”工作台：可选择包对外提供的项目字体、字体组合和图标集；字体组合依赖的成员字体会随包保存，但只有明确选择的字体资源会列为包的公开能力；图标集只保存所选定义和所需精灵图，并在包信息中提供图标数量摘要；所选项目图片会按原目录随包保存，不会夹带其他图片。首次打开时默认选中项目的全部可用资源，制作后再次打开会沿用上次的包名称、版本和内容选择。
- 窗口标题栏现在会以内嵌提示显示即时操作结果，悬停标题栏时可重新查看当前提示。
- 悬停标题栏中的即时提示区域时，当前提示会从原位置展开为包含全部历史消息的浮窗，并可在设置中调整历史消息保留上限。
- 导出的 `.ocblock` 现在可以重新导入项目；块 Key 会按独立结构定义格式保存，不再携带 `block:` 身份前缀。

- 资源引用现在统一支持当前环境、宿主项目和可见包作用域；包内资源不会因为同名而误解析到其他包，非法路径和不可用资源会稳定显示为不可用。
- 图片块、Markdown 图片、字体文件和图标精灵图现在都支持作用域简写路径：普通路径读取当前项目或包，`@路径` 读取当前项目，`包Key@路径` 读取当前作用域的子包；制作包时会自动收集跨包依赖并改写为可独立使用的包内路径。
- 卡面设计器的文件路径补全现在会列出可用的包，可直接进入包内选择文件，并通过 `..` 返回上一级；包入口显示名称，选择后自动写入包引用路径。
- 富文本中的字体和图标引用现在使用统一资源语法校验，包限定引用不会被误当作当前项目资源。
- 卡面字体引用现在也按统一资源规则解析，支持包限定字体并隔离同名字体。
- Markdown 和富文本中的图标现在会逐个按其实际来源解析，同一段内容可以混用项目图标和包图标。
- 卡面设计器的字体补全现在先选择当前项目或包，再选择其中的字体，可通过 `..` 返回项目与包列表，支持连续键盘选择并保留分号分隔的回退字体；富文本字体列表也会显示可用的包字体。补全候选只显示名称，并使用对应字体预览，包列表更新后候选随之更新；字体列表不再附带固定的六种系统字体，已有字体设置不受影响。
- 使用电脑本地安装的字体时，问题列表现在会提示该字体在其他开发者的电脑上可能无法正常显示；系统字体仍可继续保存和渲染，通用字体族不会触发提示。
- 自定义块导出分析现在会复用统一的资源引用规则，字体和图标依赖会按实际引用自动纳入资源清单。
- 自定义块导出时会保留作者明确选择的动态资源声明；重新注册 `.ocblock` 后这些声明仍然可用。
- 自定义块依赖分析现在会记录每项资源的引用来源；宿主项目引用会生成依赖诊断，包限定引用会记录对应的子包依赖。
- 新增 `.ocpack` 安装能力：打开、拖入或从包管理页导入时，会先检查归档大小、路径、内容摘要及字体和图标完整性；同 Key 的不同版本会同时显示新旧版本并询问是否替换，安装结果会记录到项目包清单。
- 运行时展开自定义块时会按所属资源环境解析嵌套块和资源；公开字段覆盖会经过安全校验，嵌套内容继续使用普通卡面渲染和字段诊断。
- 自定义块现在以纯 JSON `.ocblock` 结构文件注册和分享；导入后会登记到项目的 `.opencard/blocks/blocks.json`，块定义不再携带资源字节、发布者或版本信息。
- 包使用独立的 `.ocpack` 运输格式；资源、共享块和版本信息不再混入单个 `.ocblock` 文件。
- 新增包构建工作台：可以选择多个自定义块和项目共享资源，填写包名称、Key 与版本，并将内容保存为 `.ocpack`。
- 自定义块导出窗口现在以“块结构”和“预览与属性”分页组织工作：被块引用的字段会自动开放给使用者，字段与资源使用一致、清晰的类型和选择图标；可在沿用源卡面尺寸的即时预览中调整公开字段示例值、选择图片、字体、图标和包内块并检查缺失资源，属性修改和资源排除都会立即反映在预览中；字段和资源编辑区可通过“高级设置”展开，分页始终可切换返回。
- 在 CDE 或富文本中编辑自定义块时，属性面板会按照结构文件的字段定义提供与普通块一致的体验；块结构、结构字段、自定义字段和真正未识别的字段会分别显示，缺失字段不会从结构默认值反填，结构引用也会以“结构”显示在内容分类中。

## 改进
- 项目相关编辑器页面现在把按钮统一放在页面右上角：字体页的“添加字体/添加组合”和图标页的“创建图标包/导入图标包”从左侧列表的标题行移到了与包管理页一致的位置。
- “打开的编辑器”中的条目现在可以显示比文件名更贴切的标题（例如无对应文件的管理类编辑器），未设置标题时仍显示文件名；未保存的星号标记、外部文件与草稿前缀都会继续保留，重命名或另存为后条目重新以文件名为准。
- 输出面板现在只显示 OpenCard 各模块主动告知你的消息，不再混入开发者调试日志和未处理的错误；每条记录统一显示时间、级别和内容，并按级别区分文字与底色，失败记录会直接给出具体原因，级别改为信息、成功、警告和错误。
- 项目包清单现在只记录跨设备协作所需的包 Key、版本和来源；包管理页只显示清单中的声明，可按需检查实际目录和版本，移除声明时不会删除包文件。
- 卡面使用包内资源时，问题列表现在会分别提示包不存在和资源文件不存在，便于检查包是否完整。
- 图标页的列表和属性区域现在均分可用宽度，窄窗口下属性自动上下排列，避免图标列表被挤窄。
- 导出项目模板时，窗口标题栏现在会显示全局进度提示，导出完成或失败后自动收起。
- 设置中的主题导出现在会直接复制 JSON，导入时可在标准弹窗中粘贴 JSON，不再需要创建主题文件。
- 路径、字体和图标的分级补全现在可以使用 Shift+Tab 直接返回上一级；位于根级时仍会正常切换到上一个输入项。
- 设置页的自动保存间隔、编辑器历史记录、自定义块最大深度和标题栏即时消息上限现在显示关键数值刻度，调整范围更直观。
- 工作区左下角现在与左上角使用一致圆角；侧边栏折叠后，左下角会随工作区贴边变为直角，侧边栏宽度调节线的上下两端也会对称渐隐。
- 底部问题面板的浮动开关现在收紧了右下角，并通过边框和图标颜色表达问题级别，不再用整块背景着色。

- 树形列表的行高光现在带有小圆角，选中、悬停和拖放状态的轮廓更加柔和一致。
- 软件启动加载期间会立即显示与应用内一致的 OpenCard 标志和主题背景，完整界面准备好后启动画面会平滑淡出。
- 启动时自动检查更新失败现在只显示警告；手动检查更新后会明确提示已是最新版、发现新版本或检查失败。
- 主题 JSON、包、工作簿、项目导出和常用文件操作的结果现在统一显示在标题栏即时消息中；可恢复的错误不再弹出系统消息框，仍需确认的包升级会继续等待用户选择。
- 深浅主题切换时，背景、边框和强调色会平滑渐变，进度着色器会立即切换到新配色，前景文字与图标会在过渡中点切换，并通过标题栏即时信息确认当前主题；颜色变化不再打断侧边栏展开、浮窗显隐等交互动效，欢迎页和设置中的动态品牌图形也会保持正常显示。
- 外观设置现在可以调节相位动画速度，欢迎页和其他动态品牌图形会统一按新的速度运行。

- 问题列表现在支持父级问题与子问题层级；自定义块错误会展开显示其内部诊断，并保留逐条定位和复制能力。

- 问题列表现在可直接复制每条错误的完整 JSON 诊断信息，便于反馈和排查自定义块渲染问题。

- 自定义块在卡面和富文本中现在直接渲染为实际块内容，并统一使用块引用与公开参数值；内部块不会作为可编辑属性暴露，嵌套内容的资源来源也会保持隔离。

- 自定义块预览、卡面和富文本现在共享同一套渲染流程；预览会按最终包环境显示缺失资源和作用域问题，避免源项目资源掩盖导出后的不可用状态。

- 文档保存现在会完整保留编辑器内存中的字段数据；自定义块实例字段不会因为保存或重新打开文档而被存储层过滤或改写。

- 项目管理树中的字体、图标和自定义块现在只显示配置入口，不再展开其中的资源文件；项目管理也不再提供清理未使用字体或图标的操作。
- 项目管理区域现在会显示包目录，已安装的 `.ocpack` 可以从项目结构中直接查看。
- 项目管理中的固定入口名称简化为项目、字典、字体、图标、自定义块和包，并在中英文界面中使用一致的名称和包图标。
- 自定义块导出工作台的分页现在位于高级设置区域，默认界面更紧凑；块结构的宽度和高度现在按普通字段的公开状态保存，未公开时会写入只读约束。
- 已安装自定义块页面现在将公开字段属性固定显示在预览右侧，便于一边查看效果一边调整示例值；列表信息也更加精简，不再重复显示安装路径。
- 包制作工作台现在将包名称、Key 和版本输入集中到右侧预览区域，打开时会自动使用项目名称，并在 Key 留空时提示由名称生成的默认值。
- 自定义字段现在默认可以绑定并供其他字段引用；属性面板中的只读、隐藏和输入限制不再改变字段的绑定能力。
- 块的显示规则现在与属性面板配置相互独立；调整附加字段的编辑控件、范围或选项，不再改变宽高、颜色等内置属性的实际显示与异常值回退结果。
- Git 变更列表现在会在文件名右侧稳定显示状态图标：新增显示绿色加号，删除显示红色减号，修改显示黄色圆点；窄侧栏中的长文件名会正确省略，悬停时可查看完整名称。连续文件变化会合并刷新，切回应用或打开版本控制面板时也会自动同步。刷新失败时会保留上次结果并标记为过期。尚未启用 Git 的项目会隐藏时间线和版本树，改为提供“初始化仓库”按钮；初始化时可以设置提交者信息，并可选择立即创建包含当前项目文件的首次提交。
- 编辑器现在可以直接并排查看图片、项目字体与图标配置文件、字体文件的前后版本；图片对比支持同步缩放、平移和像素化预览，便于确认资源变化。
- 侧边栏中的空列表现在会在可用区域中央说明内容来源，例如打开的项目文件会出现在“打开的编辑器”中，当前文件的提交记录会出现在“时间线”中。
- CDE 数据表的预览浮窗不再显示选中块，并会始终显示在固定表头之上。
- CDE 现在会分别记住卡牌正面与背面的块选中状态，切换编辑器或翻转卡面后可以回到对应的编辑上下文。
- CDE 现在可以通过结构树右键菜单或 Ctrl/Cmd+C、Ctrl/Cmd+V，在不同卡面和打开的编辑器之间复制粘贴块，并保留完整嵌套结构。
- CDE 粘贴块时不再弹出浏览器剪贴板授权询问。
- 改进多个右键菜单的操作分组，让编辑、复制、移动和删除等操作更容易区分。
- 调整侧边栏宽度时会即时跟随拖拽，并保持在合理范围内，不再出现滞后的过渡动画；拖拽触发展开或收起时仍会平滑切换。
- 界面中的名称、状态、版本和路径等组合信息现在改用空格分隔，文字显示更简洁。

## 改进

- 自定义块属性面板现在直接依据块资源自身的字段定义和公开字段配置显示属性；公开的宽度字段不再被通用隐藏规则错误遮挡，未公开字段仍不会出现在编辑器和运行时覆盖中。

## 修复

- 图片块的来源现在既可使用项目图片或远程图片，也可填写 `icon:系列/图标` 或 `包Key@icon:系列/图标`，并沿用完整显示、裁切填满和拉伸设置。
- 图片块在资源环境刷新后不会再把已经加载的本地图片误显示为不可用占位。
- 空白文本块和 Markdown 文本块不再将空内容误报为缺失字段。
- 软件启动加载期间会立即显示与默认主题一致的深色背景，不再反复露出刺眼的白色底层。
- 颜色选择器中的颜色值与通道文字现在会正确使用当前主题字体。
- CDE 的卡牌树、预览、结构树和属性面板标题现在会正确跟随界面语言切换。

- 打开包制作工作台时不再因翻译提示反复刷新页面或持续刷出大量警告。

- 切换预览文件时，文档编辑器现在可以正常打开，不再因旧预览释放时的异常而中断。

- 在卡面或结构树中选择块时不再因重复回写同一选区而卡顿或触发递归更新错误。

- 问题列表中的富文本诊断现在会显示正确的中文或英文说明，不再显示内部翻译键。

- 自定义块相关问题现在会显示实际块名称，不再统一显示为“未命名块”。

- 卡面绑定现在不会再把正常的布尔值和数字值误报为类型错误，字段最终仍由渲染阶段负责校验和回退。

- 自定义块内部的文本内容现在会正常渲染，项目字体、绑定文字和项目图标都会与普通卡面内容保持一致，不再只显示图片等非文本子块。

- 自定义块渲染诊断不会再触发画布递归更新，打开包含内部错误的文档时不再持续刷出 `Maximum recursive updates exceeded`。

- 自定义块现在可以通过点击画布内容正确选中外层块。
- 所有块的宽高调整手柄现在根据实例是否拥有 `width`、`height` 字段显示，不再受字段当前值是否已定义影响。

- 卡面中的容器块和以容器为根的自定义块会重新按照设置的 X/Y 与锚点准确定位，嵌套内容不再整体偏移。
- 打开含有已移除或不完整块记录的卡牌文档时，编辑器会将其稳定显示为通用不可用节点，不再因结构树渲染异常造成工作区空白或界面卡死。
- 打开卡牌文档时，侧边栏尺寸测量不会再反复写回相同值，避免首屏触发递归更新并卡住编辑器。
- 删除包含子块的结构后，会同步清理所有卡牌实例中对应的无效字段覆盖数据，避免实例数据残留已不存在的块。
- 重新安装与已有结构使用相同 Key 的 `.ocblock` 时，会替换旧结构，不再因旧文件残留而继续使用旧版本。
- 在属性面板中新增字段时，初始值现在由编辑器宿主统一生成；自定义块会优先使用结构根节点中的默认字段值。

- 已安装自定义块页面的列表和预览区域现在会正确填满可用高度，不再挤在页面顶部。
- CSS 长度字段现在可以正确接受负数值，不再错误显示渲染警告。
- 树形列表的操作浮窗现在会直接出现在触发位置，不再从窗口左上角闪现。
- 未选择项目图标集时，右侧的提示现在会正确显示在可用区域中央。
- 侧边栏树在节点内容与空状态提示之间切换时会保持可用高度，刷新时间线等内容时不再引起面板抖动。
- 未启用 Git 的项目不再短暂显示“正在加载时间线”，打开或切换编辑器时侧边栏中的其他列表也不会因此跳动。
- 新建文本块的空富文本不再包含额外的换行。
- 底部面板的圆形控制按钮现在固定在窗口右下角并始终显示在编辑器内容上方，编辑卡牌内容时不会再影响画布区域的位置或被画布遮挡。
