`<模块路径>` 是“<模块定位>”，不是“<容易混淆的定位>”：
- 输入：<输入协议/关键 props/依赖>。
- 输出：<事件/返回值/副作用接口>。
- 负责：<本模块职责边界>。
- 不负责：<必须留给外层的业务决策>。

通信与协议约束：
- 只通过 <稳定标识符> 通信，不传 <可变对象/隐式上下文>。
- 事件/接口 payload 使用 `<字段1, 字段2, ...>`，禁止回退到 `<旧协议>`。
- <额外协议约束 1>。

状态真相约束：
- 真相源：<唯一状态来源>。
- 展示投影：<如何物化/回退默认值>。
- 禁止：<双轨状态/隐式同步/偷改外部对象>。

扩展与兼容边界：
- 允许扩展：<可扩展点，如 schemaOverride/datatype 注册>。
- 禁止扩展：<不应在本层吸收的规则>。
- 历史兼容：不保留 `<旧字段/旧事件/旧别名>`。

调用方跟随约束：
- 调用方必须通过 <稳定 id> 路由，不依赖 <文案/对象身份>。
- 调用方负责 <合法性校验/持久化/domain 规则>。

不可回退约束：
- 不得回退到 `<旧接口 A>`。
- 不得重新引入 `<隐式默认语义/兼容桥>`。

重构原则（本模块）：
- 先收紧契约，再改实现，再改调用方。
- 每次重构只消灭一个隐式契约，不混入无关改写。
