`AppIcon.vue` 是图标系统的唯一渲染入口。它存在的意义不是少写几行模板，而是把“图标源、尺寸、颜色、tone”从业务组件中抽走。

不要让页面和业务组件重新直接渲染 `codicon` class 或手写 SVG。那样会立刻失去统一换肤、统一着色、统一迁移图标库的能力。

如果未来需要 hover/active/disabled 状态色、主题切换、品牌色图标，优先继续扩展这里和全局 token，不要在调用点写分散的内联样式。
