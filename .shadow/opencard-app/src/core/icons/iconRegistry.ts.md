`iconRegistry.ts` 的职责是“图标语义键 -> 图标实现定义”，而不是让业务层直接依赖某个图标库。

这里要长期保持多图标源能力。当前支持 `codicon` 和 `mdi`，以后如果换 SVG 资源或品牌图标，也应该继续在这一层吸收，而不是把 `<i class="...">` 或 path 字符串重新散落回业务组件。

颜色和尺寸不是注册表的真相；注册表描述“是什么图标”，具体显示策略交给 `AppIcon` 和样式 token。
