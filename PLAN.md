# PRD：可安装、可嵌套的自定义块包系统

> 目标文件：`tasks/prd-custom-block-package-system.md`

## 1. Introduction / Overview

将 OpenCard 自定义块从“项目内登记并按需解压 `.ocblock` ZIP”改为“`.ocblock` 仅用于分发，安装后展开为项目内自包含目录”。

每个已安装自定义块由 `manifest.json`、`block.json` 和 `resources/` 组成；其中 `resources/` 不是扁平资源袋，而是一个只保留必要资源的最小只读 OpenCard 项目投影。自定义块渲染时复用普通项目的资源加载机制，只切换资源作用域根目录。

自定义块可以继续引用其他自定义块。被依赖块递归内嵌在上层块的 `resources/.opencard/blocks/` 中，使导出包无需联网或解析外部依赖即可独立运行。

导出工作流升级为分页“包构建工作台”：包内容页上方配置包信息，下方并排展示字段可用性树与包内资源树；预览与属性页使用空白宿主项目加载当前候选包，左侧实时预览安装到其他项目后的真实效果，右侧通过 PropertyEditor 调整公开字段的预览覆盖值。发布者 Key 由应用首次启动时生成并持久化，导出界面不显示也不允许编辑。

## 2. Goals

- 让项目内已安装自定义块以可查看、可理解的目录形式存在，而不是运行时 ZIP。
- 让普通块和自定义块复用同一套图片、字体、图标和富文本资源加载机制。
- 让 `.ocblock` 只承担导入、导出、上传、分享和下载的运输职责。
- 使用不可变的 `publisher-key/block-key` 作为包身份，避免不同作者之间的块 Key 冲突。
- 支持 A 包含 B、B 包含 C 的递归自定义块组合，并保证导出包离线、自包含。
- 自动分析资源依赖，同时允许用户通过项目资源树增删最终写入包内的资源。
- 使用真实候选包和空白宿主环境预览导出结果，避免当前项目资源掩盖遗漏。
- 删除 `.ocblocks` 注册表、项目内 `.ocblock` 运行时和旧资源索引，不保留兼容分支。

## 3. User Stories

### US-001：定义新的包身份与目录模型

**Description:** 作为自定义块作者，我希望每个包拥有全局可区分且可读的身份，以便不同作者可以发布同名块。

**Acceptance Criteria:**

- [x] Package ID 格式固定为小写 `publisher-key/block-key`。
- [x] 两个 Key 分段均使用共享 `keySlug.ts` 生成和校验，并按不区分大小写处理冲突。
- [x] manifest 包含 `packageId`、`version`、`name`、公开字段和尺寸策略。
- [x] `version` 为必填 SemVer 字符串；新导出默认 `0.1.0`。
- [x] 已安装目录固定为 `.opencard/blocks/<publisher-key>/<block-key>/`。
- [x] 文档中的自定义块实例持久化完整 Package ID，而不是仅保存块名。
- [x] 类型检查与相关模型单元测试通过。

### US-002：建立新的 `.ocblock` 包结构

**Description:** 作为包作者，我希望导出包结构与安装目录一致，以便包容易检查、安装和重新导出。

**Acceptance Criteria:**

- [x] `.ocblock` 保持 ZIP 兼容运输格式。
- [x] ZIP 根目录只包含当前包允许的结构：`manifest.json`、`block.json`、`resources/`。
- [x] `resources/` 被视为迷你项目根目录，内部资源保持源项目相对路径。
- [x] 项目图片 `assets/a.png` 导出后位于 `resources/assets/a.png`，且 `block.json` 中路径不改写。
- [x] 包内字体通过裁剪后的 `resources/.opencard/.ocfonts` 及对应字体文件工作。
- [x] 包内图标通过裁剪后的 `resources/.opencard/.ocicons` 及对应图集工作。
- [x] manifest 不再保存旧的图片、字体、图标字节索引。
- [x] 类型检查与包读写单元测试通过。

### US-003：安全、原子地安装和更新包

**Description:** 作为项目使用者，我希望安装或更新自定义块时不会留下半写入目录，也不会允许不安全路径逃出项目。

**Acceptance Criteria:**

- [x] 导入前检查压缩包总大小、解压大小、文件数、单文件大小和路径安全。
- [x] 拒绝绝对路径、`..` 路径、重复路径、符号链接及逃出包根目录的资源引用。
- [x] 包先解压到同一文件系统中的临时安装目录。
- [x] 完成 manifest、块定义和结构校验后，才原子替换最终 Package ID 目录。
- [x] 更新失败时恢复旧安装目录，不留下混合版本。
- [x] 同 Package ID 的导入被识别为更新；不同 Package ID 不互相覆盖。
- [x] 卸载以整个 Package ID 目录为单位，并使引用它的实例显示不可用诊断。
- [x] 类型检查与安装、更新、回滚、安全路径测试通过。

### US-004：按目录发现并按需加载自定义块

**Description:** 作为项目使用者，我希望项目打开快速，同时所有已安装块都能被自动发现。

**Acceptance Criteria:**

- [x] 项目打开时扫描 `.opencard/blocks/<publisher>/<block>/manifest.json`，不读取 `.ocblocks`。
- [x] 初始发现阶段只读取 manifest；只有块被插入、渲染、预览或检查时才读取 `block.json` 和必要资源。
- [x] 目录缺少部分语义内容时保留可用 manifest 投影并显示诊断，不使整个项目打开失败。
- [x] 结构完全不可用、路径不安全或 JSON 无法读取时，该包标记为不可用。
- [x] 文件监听将任意已安装包目录内的变化映射到对应 Package ID，并使其解析模型和运行时会话失效。
- [x] 删除旧的“解压字节 Map + 按包 LRU”运行时缓存；仅缓存解析模型和可释放的字体/图标会话。
- [x] 类型检查与目录发现、按需加载、监听失效测试通过。

### US-005：统一项目与自定义块资源环境

**Description:** 作为渲染系统维护者，我希望普通项目与自定义块使用相同资源解析合同，以减少专用分支。

**Acceptance Criteria:**

- [x] 提供统一的 Project Resource Environment，可从任意项目式根目录加载图片、字体、图标和富文本资源。
- [x] 普通文档的默认资源根为当前项目目录。
- [x] 自定义块定义的默认资源根为其安装目录下的 `resources/`。
- [x] 图片路径保持普通项目相对路径，并通过对应资源环境转换为 Tauri 可加载 URL。
- [x] 包内字体与图标运行时名称按 Package ID 隔离，宿主项目中的同名 Key 不覆盖它们。
- [x] 删除 `resource:image:*`、`resource:font:*` 和对应专用路径解析分支。
- [x] 类型检查与同名图片、字体、图标跨作用域隔离测试通过。

### US-006：保留字段级资源来源

**Description:** 作为自定义块使用者，我希望包内默认资源和宿主项目公开字段覆盖资源都能从正确位置加载。

**Acceptance Criteria:**

- [x] 渲染阶段以 `blockId + fieldKey` 记录字段资源作用域，而不是仅给整个子树一个根目录。
- [x] 从包定义复制的字段使用当前自定义块的 `resources/`。
- [x] 宿主实例显式覆盖的公开字段使用宿主项目资源环境。
- [x] 新建自定义块实例只持久化显式覆盖值，不复制所有公开字段默认值。
- [x] PropertyEditor 显示包内有效默认值，但用户执行“恢复默认”时删除实例覆盖字段。
- [x] 嵌套块公开字段被上层包覆盖时，覆盖值保留上层包资源作用域；被最终宿主覆盖时使用宿主作用域。
- [x] 图片、字体、图标和富文本内容均遵守字段资源作用域。
- [x] 类型检查与默认值、一级覆盖、多级嵌套覆盖测试通过。

### US-007：递归内嵌自定义块依赖

**Description:** 作为包作者，我希望自定义块能够组合其他自定义块，并在离线安装后保持相同效果。

**Acceptance Criteria:**

- [x] 导出不再把嵌套自定义块烘焙为原生块。
- [x] 包读取不再删除合法的嵌套自定义块节点。
- [x] A 引用 B 时，将 B 的完整安装目录写入 `A/resources/.opencard/blocks/<B publisher>/<B block>/`。
- [x] B 已内嵌 C 时，B 自己的 `resources/` 结构保持不变。
- [x] 同一依赖在同一资源环境中只写入一次。
- [x] A 使用自己内嵌的 B，不被宿主项目中同 Package ID 的其他版本替换。
- [x] 检测 Package ID 祖先链循环，并对 A→B→A 给出循环诊断。
- [x] 执行递归深度、依赖数量、文件数量和总体积上限。
- [x] 类型检查与至少三级嵌套、重复依赖、版本隔离和循环测试通过。

### US-008：分析候选资源并建立可编辑资源树

**Description:** 作为包作者，我希望导出器自动勾选能够识别的资源，同时允许我补充动态引用或移除不需要的文件。

**Acceptance Criteria:**

- [x] 导出分析遍历待导出块树、所有字符串字段和富文本内容。
- [x] 自动识别合法项目相对图片路径、`font:<key>`、标准项目图标引用和嵌套 Package ID。
- [x] 字体组合递归包含成员字体；图标选择生成必要的裁剪注册表和图集。
- [x] 对含动态片段但具有静态图标系列前缀的引用提供“动态引用建议”。
- [x] 无法识别的动态引用不会阻止用户手动选择对应资源。
- [x] 资源树保持项目目录层级，只显示图片、注册字体、注册图标及嵌套自定义块等可打包资源。
- [x] 资源树复用标准 `OcTree`；文件夹和资源节点通过“选入包内/从包内排除” action 批量修改后代选择，并标识自动识别、动态建议、嵌套依赖、手动添加、缺失和排除状态。
- [x] 用户取消自动选中项后，系统不得在导出时偷偷补回。
- [x] 嵌套自定义块目录作为原子依赖节点选择，其内部包不在当前导出器中被局部修改。
- [x] 类型检查与静态引用、动态引用、手动选择和取消选择测试通过。
- [ ] 使用 dev-browser skill 在浏览器验证资源树交互。

### US-009：重做自定义块导出工作区

**Description:** 作为包作者，我希望在同一个工作区内配置包信息、公开字段和资源，以便完整检查导出内容。

**Acceptance Criteria:**

- [x] 使用 `OcDialog` 的 `xl` 宽度和语义工作区高度预设，不实现自定义 modal 基础设施。
- [x] 对话框使用 `OcOptionGroup` 分为“包内容”和“预览与属性”两页。
- [x] 包内容页上方包含名称、块 Key 和版本输入；不可变发布者 Key 由应用设置提供，界面不显示也不可编辑。
- [x] 包内容页下方并排展示公开/私有字段树与包内资源树，优先提供纵向空间。
- [x] 包内资源树始终显示字体、图标、子包和其他项目图片四个根分类，并提前选中可静态识别的资源。
- [x] 预览与属性页左侧为 CardViewport 最终效果预览。
- [x] 预览与属性页右侧为贴边、只包含公开字段的 PropertyEditor。
- [x] 预览页显示候选包结构、资源缺失、嵌套循环及渲染失败诊断。
- [x] 所有新增或修改文案同步更新全部支持语言。
- [x] 样式只使用现有语义 token 和共享组件几何；缺失值先加入 foundation/theme。
- [x] 类型检查、组件测试和交互测试通过。
- [ ] 使用 dev-browser skill 在浏览器验证布局、滚动、缩放、键盘操作和不同主题。

### US-010：使用真实候选包进行隔离预览

**Description:** 作为包作者，我希望预览准确反映当前设置导出后安装到其他项目的效果。

**Acceptance Criteria:**

- [x] 每次字段可用性、资源选择、Package ID 或版本变化后，防抖构建新的候选包。
- [x] 候选包使用与正式导出相同的 manifest、block、最小资源项目和递归依赖构建逻辑。
- [x] 预览通过正式安装目录读取器和正式渲染管线加载，不使用专用假数据资源解析器。
- [x] 预览宿主为空白项目，只提供 OpenCard 内置能力，不继承当前项目字体、图标或图片。
- [x] 旧异步预览结果不得覆盖较新的设置。
- [x] 用户取消必要资源后，预览立即显示与实际安装一致的缺失效果和诊断。
- [x] 资源语义缺失不被自动修复；只对路径逃逸、损坏结构或无法形成块投影等硬错误阻止导出。
- [x] 对存在资源诊断但仍可形成包的情况，导出前使用标准 `OcDialog` 明确确认；确认后允许导出。
- [x] 类型检查、候选包一致性和异步竞态测试通过。
- [ ] 使用 dev-browser skill 在浏览器验证预览刷新、错误态和确认流程。

### US-011：通过 PropertyEditor 模拟公开字段覆盖

**Description:** 作为包作者，我希望在导出前调整公开字段预览值，以验证使用者配置该块后的效果。

**Acceptance Criteria:**

- [x] PropertyEditor 只显示当前设置为公开的字段。
- [x] 初始有效值来自候选包 `block.json`。
- [x] 用户编辑值只写入预览实例，不改变源块或候选包默认值。
- [x] “恢复默认”删除预览实例覆盖并恢复包内有效值。
- [x] 公开字段列表变化时保留仍然有效的预览值，并丢弃已设为私有的预览覆盖。
- [x] 在空白宿主中引用宿主资源的覆盖值按缺失资源展示，不回退到当前源项目。
- [x] 类型检查与 PropertyEditor 状态测试通过。
- [ ] 使用 dev-browser skill 在浏览器验证字段编辑、恢复默认和预览联动。

### US-012：让所有渲染表面使用新运行时目录

**Description:** 作为使用者，我希望设计器、展示器、批量渲染和富文本嵌入在同一包上得到一致结果。

**Acceptance Criteria:**

- [x] 卡牌设计器从项目安装目录加载活动 Package ID。
- [x] 普通卡牌展示、导出渲染和自定义块管理器预览使用同一 Runtime Catalog。
- [x] 富文本输出中的 inline/block 自定义块使用同一嵌套包解析和资源作用域。
- [x] 富文本编辑器节点至少持续显示可用、缺失和加载中状态；真实所见即所得节点预览不作为本故事的必需范围。
- [x] 缺失包、资源缺失和循环依赖在各渲染表面产生一致诊断。
- [x] 类型检查与设计器、展示器、批量渲染、富文本嵌入测试通过。
- [ ] 使用 dev-browser skill 在浏览器验证设计器、管理器和富文本编辑状态。

### US-013：更新自定义块管理与项目集成

**Description:** 作为项目使用者，我希望直接管理已安装目录中的包，而不是编辑路径注册表。

**Acceptance Criteria:**

- [x] 原自定义块注册表编辑器改为安装包管理器，不再编辑 `.ocblocks` 文本。
- [x] 管理器列出 Package ID、名称、版本、状态和安装位置。
- [x] 支持从 `.ocblock` 安装、更新、卸载、在文件管理器中显示目录及预览。
- [x] 项目文件树将 `.opencard/blocks/<publisher>/<block>/` 显示为包目录。
- [x] 项目模板导入、创建和导出复制展开后的安装目录，不在模板中嵌套项目运行时 `.ocblock`。
- [x] 版本对比快照直接读取安装目录和迷你资源项目。
- [x] 外部打开 `.ocblock` 仍显示运输包信息并提供安装操作。
- [x] 类型检查与管理器、模板、快照、外部打开测试通过。
- [ ] 使用 dev-browser skill 在浏览器验证安装、更新、卸载和预览流程。

### US-014：删除旧模型并完成用户可见收尾

**Description:** 作为维护者，我希望代码库只保留一个当前自定义块模型，避免双重真相和长期兼容负担。

**Acceptance Criteria:**

- [x] 删除 `.opencard/.ocblocks` 默认文件、解析器、序列化器和文件类型。
- [x] 删除项目内 `.ocblock` 注册路径、旧 `archivePath` 语义和相关编辑器分支。
- [x] 删除旧 ZIP 运行时解压、字节 Map、Blob 资源索引和旧资源引用重写逻辑。
- [x] 删除导入时移除嵌套自定义块和导出时拍平嵌套块的逻辑。
- [x] 不添加旧项目自动迁移、双格式读取或 schema 版本分支。
- [x] 更新全部相关单元测试、组件测试和用户文档。
- [x] 更新 `opencard-app/RELEASE_NOTES.md`，用面向用户的中文描述安装目录、嵌套块和新导出预览能力。
- [x] 运行聚焦测试、类型检查和 lint；开发验证不得运行 `npm run build`。

## 3.1 Verification Record

- `npx vue-tsc --noEmit --pretty false`：通过。
- 自定义块包、安装回滚、资源环境、递归导出、候选预览、导出工作台、管理器、模板、快照、富文本与渲染相关聚焦测试：通过。
- `npm run lint:naming`、`npm run lint:ui`：通过。
- 全量 Vitest 已尝试两次；当前环境在 10 分钟限制前未完成，`--bail=1` 定位到的首个失败是并发压力下无关的 `cardDataWorkbook.spec.ts` 5 秒超时，而不是断言失败。
- dev-browser 验收仍未勾选：补装兼容二进制并启动独立 Vite 服务后，`agent_browser` 仍以 `net::ERR_CONNECTION_CLOSED` 无法访问本机开发地址。
- `cargo.exe test external_open --quiet`：通过（1 项）。

## 4. Functional Requirements

- **FR-1:** 系统必须使用小写 `publisher-key/block-key` 作为不可变 Package ID。
- **FR-2:** Package ID 分段必须通过共享 Key slug 与不区分大小写的唯一性逻辑处理。
- **FR-3:** `.ocblock` 必须是运输 ZIP；项目运行时不得直接从 ZIP 加载资源。
- **FR-4:** 项目安装根必须为 `.opencard/blocks/<publisher-key>/<block-key>/`。
- **FR-5:** 已安装包必须包含 `manifest.json`、`block.json` 和 `resources/`。
- **FR-6:** `resources/` 必须实现最小只读项目根合同，保持源项目资源相对路径。
- **FR-7:** 包内字体和图标注册表必须由导出器按当前选择生成，不直接复制完整项目注册表。
- **FR-8:** 项目启动必须通过目录发现包，不得依赖 `.ocblocks` 注册表。
- **FR-9:** 系统必须先读 manifest，并仅在实际使用时加载块定义和资源。
- **FR-10:** 安装必须在临时目录中完成校验，再原子替换最终目录。
- **FR-11:** 安装器必须拒绝不安全归档路径、符号链接和资源根逃逸。
- **FR-12:** 普通项目和自定义块必须使用同一种资源环境接口。
- **FR-13:** 自定义块资源环境必须以 `<installed-package>/resources/` 为根。
- **FR-14:** 字段资源来源必须在渲染模型中按 `blockId + fieldKey` 保存。
- **FR-15:** 未被实例覆盖的字段必须使用定义它的包资源作用域。
- **FR-16:** 实例显式覆盖值必须使用写入该值的宿主资源作用域。
- **FR-17:** 自定义块实例不得复制并持久化全部公开字段默认值。
- **FR-18:** 嵌套自定义块必须保留为自定义块节点，不得导出拍平。
- **FR-19:** 被依赖包必须递归内嵌在当前包 `resources/.opencard/blocks/` 中。
- **FR-20:** 每个被内嵌包必须保持自己的 manifest、block 和 resources 边界。
- **FR-21:** 系统必须检测并报告递归 Package ID 循环。
- **FR-22:** 导出分析器必须自动识别静态图片、字体、图标和嵌套块依赖。
- **FR-23:** 导出分析器应对可识别静态前缀的动态引用提供建议，但不得声称已确定完整资源。
- **FR-24:** 资源树必须允许用户手动选择和取消选择资源。
- **FR-25:** 用户取消自动资源后，最终候选包和导出包必须尊重该选择。
- **FR-26:** 缺失资源必须通过真实候选包预览和诊断暴露，不得静默补齐。
- **FR-27:** 资源语义缺失应允许用户确认后导出；不安全或无法形成可用块投影的结构必须阻止导出。
- **FR-28:** 导出对话框必须使用 `OcDialog`，左侧配置，右侧预览与 PropertyEditor。
- **FR-29:** 预览必须使用空白宿主项目和正式包加载/渲染链路。
- **FR-30:** PropertyEditor 的修改必须仅作用于预览实例覆盖值。
- **FR-31:** 管理器、设计器、展示器、导出渲染和富文本必须消费同一 Runtime Catalog。
- **FR-32:** 项目模板和版本对比必须读取展开后的安装目录。
- **FR-33:** 所有新增或变更文案必须同步更新全部支持语言。
- **FR-34:** 所有用户可见改动必须更新下一版发布说明。

## 5. Non-Goals / Out of Scope

- 不实现公共市场的上传、搜索、下载、评分或审核后端。
- 不实现发布者实名认证、包签名、来源收据或完整性验证 UI。
- 不引入内部 UUID；首版以不可变 `publisher-key/block-key` 作为唯一身份。
- 不实现外部依赖解析、在线依赖下载、语义版本范围或依赖提升。
- 不允许包执行脚本、生命周期 Hook 或任意代码。
- 不实现旧 `.ocblocks` 项目的迁移或兼容读取。
- 不长期并行支持项目内 ZIP 注册与目录安装两种模型。
- 不要求富文本编辑器节点在本期实现完整实时所见即所得渲染；正式富文本输出必须支持。
- 不允许在导出 A 时直接修改被依赖包 B 的内部文件；B 作为原子包内嵌。
- 不扩展为一个包导出多个顶级自定义块。

## 6. Design Considerations

### 6.1 导出对话框

- 使用 `OcDialog` 的标准标题、描述、焦点、关闭和高度合同。
- 左侧按“包信息 / 字段可用性 / 资源”形成清晰分区。
- 右侧使用 CardViewport，并复用 `OcViewportInspector` 或同类共享检查器承载 PropertyEditor。
- 资源目录采用三态树；自动识别项使用现有 tone、图标和 tail 文本表达来源，不使用魔法颜色。
- 资源诊断应能定位到具体字段、资源路径或嵌套 Package ID。
- 忙碌状态不应清空当前预览；新候选包成功后原子替换预览。

### 6.2 包管理器

- 管理界面以 Package ID 为主身份，以名称为展示标题。
- 更新操作明确显示当前版本和候选版本。
- 卸载后保留文档实例，实例显示包不可用，而不是删除用户内容。

### 6.3 可访问性与本地化

- 树、预览、诊断和 PropertyEditor 必须有可读标签和键盘操作。
- 所有状态不能只依赖颜色表达。
- 所有 i18n Key 必须在全部支持语言同时新增、修改或删除。

## 7. Technical Considerations

### 7.1 建议的安装结构

```text
<project>/.opencard/blocks/alice/status-badge/
├─ manifest.json
├─ block.json
└─ resources/
   ├─ assets/card/background.webp
   └─ .opencard/
      ├─ .ocfonts
      ├─ .ocicons
      ├─ fonts/Inter-Regular.woff2
      ├─ icons/status-atlas.png
      └─ blocks/bob/block-b/
         ├─ manifest.json
         ├─ block.json
         └─ resources/...
```

### 7.2 核心边界

建议将现有 ZIP 服务拆分为三个明确职责：

1. **Transport Package Reader/Writer**：仅处理 `.ocblock` ZIP、安全预检和导出。
2. **Installed Block Repository**：目录发现、安装、更新、卸载、manifest/definition 读取。
3. **Project Resource Environment**：从项目式根目录解析图片、字体、图标、自定义块目录和富文本资源。

功能组件不得直接读取 ZIP、拼接安装路径或持有包文件字节 Map。

### 7.3 资源闭包

- 导出器从结构化字段开始分析，再执行字符串与富文本启发式分析。
- 用户选择是最终资源闭包的权威输入。
- 字体和图标的结构化注册表文件由选择结果生成；用户不直接编辑生成注册表。
- 嵌套包作为完整目录复制，不把其内部资源混入父包资源根。
- 动态引用遗漏通过预览和诊断补救，不要求静态分析器完整理解所有模板表达式。

### 7.4 预览实现

- 候选包应写入受控临时目录，以便复用真实文件 URL、字体加载和图标尺寸逻辑。
- 每次重建携带递增 revision；过期任务完成后必须释放资源且不得提交 UI 状态。
- 新候选包接管前释放旧字体、对象 URL、图标图集及文件监听会话。
- 关闭对话框后清理全部临时目录和运行时资源。

### 7.5 数据模型替换

- 移除 `customBlockKey` 的单段身份语义，使用完整 `packageId`。
- 移除 `archivePath`、`files` 和 manifest 旧 `resources` 索引。
- 移除 `.ocblocks` 文档类型和注册路径模型。
- 实例模型仅保存 Package ID、实例元数据、几何值和显式公开字段覆盖。
- 读取当前模型时继续遵循项目宽容读取原则：缺字段使用当前默认，未知字段忽略并产生诊断；不为旧模型添加特判。

### 7.6 验证方式

- 开发期间启动或复用 Vite 开发服务器，通过 HMR 和浏览器行为验证。
- 使用聚焦 Vitest、类型检查和 lint。
- 不运行 `npm run build`。
- UI 故事使用 dev-browser skill 验证真实浏览器行为。

## 8. Success Metrics

- 项目运行时不再读取或解压项目内 `.ocblock`。
- 打开项目时只读取已安装包 manifest，未使用包不读取 `block.json` 资源主体。
- 普通图片相对路径在导出前后保持不变，并能从包 `resources/` 正确加载。
- 包内字体、图标与宿主同名 Key 可以同时正确渲染。
- A→B→C 至少三级嵌套在离线空白宿主中与源项目视觉一致。
- A 内嵌的 B 不受宿主安装的另一版本 B 影响。
- 用户取消自动识别资源后，预览准确显示实际缺失结果，导出器不偷偷补回。
- 动态图标引用可通过手动勾选系列或图集在空白宿主预览中恢复。
- 代码库中不再存在 `.ocblocks` 运行时、项目 ZIP 注册路径和旧资源协议消费分支。
- 管理器、设计器、展示器、导出渲染和富文本对同一候选包产生一致结果与诊断。

## 9. Open Questions

本 PRD 范围内无阻塞性开放问题。以下议题明确延期：

- 公共市场与发布者命名空间的服务端所有权验证。
- 包签名、来源收据和完整性哈希。
- 多市场环境下是否引入不可变内部 UUID。
- 外部依赖与版本范围解析。
- 富文本编辑器节点的完整实时自定义块渲染。
