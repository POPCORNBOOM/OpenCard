# OpenCard 应用层

## 文件系统

## 编辑器系统

- 蓝图编辑器

## 渲染系统


A. 编辑器类型分层

基础编辑器：
├─ 原始 JSON 编辑器（Monaco）
├─ 表格编辑器（CSV/Excel）
└─ 文本编辑器（Markdown）

专用编辑器：
├─ 蓝图编辑器（可视化 + JSON）
├─ 数据表编辑器（带验证）
├─ 多语言编辑器（翻译辅助）
└─ 资源管理器（图片预览）

高级编辑器：
├─ 卡牌预览器（实时渲染）
├─ 批量编辑器（多卡牌操作）
└─ 导出配置器（PDF/TTS）

B. 文件关联智能化

// 自动识别文件类型，打开对应编辑器
const editorRegistry = {
'*.blueprint.json': 'BlueprintEditor',
'cards/*.json': 'CardEditor',
'i18n/*.json': 'I18nEditor',
'data/*.csv': 'DataTableEditor',
'opencard.json': 'SettingsEditor',
};

C. 编辑器间的联动

比如：
- 在蓝图编辑器中引用图片 → 自动在资源库高亮
- 在数据表中修改技能 → 相关卡牌自动标记为"需要更新"
- 在多语言编辑器中添加翻译 → 卡牌预览实时切换语言

D. 命令面板的重要性

VSCode 的命令面板是灵魂，OpenCard 也应该有：

Ctrl+Shift+P 打开命令面板：
- "新建角色卡"
- "批量导出 PDF"
- "验证所有数据表"
- "生成缩略图缓存"
- "清理未使用资源"

4. 一个有趣的问题

如何平衡"文件系统透明"和"用户体验"？

比如：
- 用户删除一张图片 → 是否自动更新所有引用它的卡牌？
- 用户重命名文件夹 → 是否自动更新路径引用？
- 用户手动编辑 JSON → 如何处理格式错误？

我的想法是：
1. 默认透明：让用户看到真实的文件操作
2. 智能提示：检测到问题时提示，但不强制修复
3. 可选自动化：提供"修复引用"等工具，但由用户决定是否使用

5. 技术栈的选择

基于你的理念，我建议：

Tauri + Vue 3 是最佳选择：
- ✅ 直接访问文件系统（不需要复杂的 Rust）
- ✅ Vue 的组件化适合多编辑器架构
- ✅ 轻量级，启动快
- ✅ 跨平台

关键库：
- chokidar - 文件监听（纯前端）
- monaco-editor - JSON/代码编辑
- konva.js - 卡牌渲染
- papaparse - CSV 处理
- vue-i18n - 多语言支持

6. 一个核心问题

你希望 OpenCard 的目标用户是谁？

- 桌游设计师：需要强大的可视化编辑
- 程序员：更喜欢直接编辑 JSON
- 美术：需要所见即所得

不同用户群体会影响编辑器的设计重点。

---
你觉得这个方向如何？有没有特别想深入讨论的点？比如：
- 蓝图系统的设计（如何定义卡牌模板）
- 数据表的验证机制
- 多语言的工作流
- 或者其他你关心的问题？